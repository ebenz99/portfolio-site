// const COLORS = ["turquoise", "pink", "yellow", "blue", "orange", "green"];
const COLORS = [1,2,3,4,5,6,7].map((color) => (`color${color}`));
const SECTIONS = ["books", "music", "photos", "lichess", "nfts", "github", "linkedin"];

module.exports = { 
    COLORS,
    SECTIONS
}